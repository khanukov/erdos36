# Changelog

## 0.1.1-preprint — 2026-08-17

- Preserve `v0.1.0-preprint` unchanged and identify its exact release, commit,
  version DOI `10.5281/zenodo.21969299`, and concept DOI
  `10.5281/zenodo.21969298`.
- Close the nested upstream-manifest checksum gap and require the exact pinned
  19-file input set, including the load-bearing aggregate and per-bin reports.
- Reject every positive gap between adjacent outer bins rather than accepting
  one within a numerical tolerance.
- Strengthen release-evidence semantic validation, strict JSON handling, and
  negative mutation coverage.
- Use supported GMP/MPFR headers and link interfaces and record the runtime
  library versions used by the central check.
- Make the manuscript more self-contained by deriving the mean range, second
  moment row, ordinary cosine row, central-bin symmetry, splice, and strict
  final implication.
- Narrow the novelty and record language: Parseval energy constraints are
  prior work, while the claimed contribution is the finite profile-space
  prefix certificate and its central-bin application. The result remains
  preliminary, unrefereed, not independently reproduced, and not Lean-verified.
- Correct the non-load-bearing floating diagnostic margin and align the
  certificate's explanatory lemma with the manuscript's `[0,2]` coordinates.
- Clarify that release archives are normalized and checksummed, but are not
  claimed to rebuild byte-for-byte because generated evidence and PDF output
  are environment-dependent.

## 0.1.0-preprint — 2026-08-16

- Record the proposed composite bound `c_E > 0.3805603`.
- Add the 150-row central Parseval-prefix certificate and C/MPFR verifier.
- Pin Price's 170 outer-bin reports by commit and SHA-256.
- Make the composite check depend on a fresh central run and verified outer inputs.
- Document the exact trust boundary, zero Lean coverage, AI assistance, and
  preliminary/unrefereed status.
- Add normalized, allowlisted release-candidate tooling and complete citation/Zenodo metadata.
